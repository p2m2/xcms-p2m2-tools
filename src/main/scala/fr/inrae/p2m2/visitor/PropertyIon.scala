package fr.inrae.p2m2.visitor

import fr.inrae.p2m2.format.XCMSFeaturesIon

case object PropertyIon {
  /*
  def retentionTime(feature: MGFFeaturesIon) : Option[Double] =
    feature.properties.get("RTINSECONDS".toLowerCase).map(_.toDouble)

  def scans(feature: MGFFeaturesIon): Option[Int] =
    feature.properties.get("SCANS".toLowerCase).map(_.toInt)

  def charge(feature: MGFFeaturesIon): Option[String] =
    feature.properties.get("CHARGE".toLowerCase)

  def msLevel(feature: MGFFeaturesIon): Option[Int] =
    feature.properties.get("MSLEVEL".toLowerCase).map(_.toInt)

  def pepMass(feature: MGFFeaturesIon): Option[Double] =
    feature.properties.get("PEPMASS".toLowerCase).map(_.toDouble)
*/
}
